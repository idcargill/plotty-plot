const circleSettings = {
	color: 'blue',
	fillColor: 'lightblue',
	fillOpacity: 0.5,
	radius: 500,
};

const pointSettings = {
	color: 'red',
	fillColor: 'red',
	fillOpacity: 0.5,
	radius: 500,
};

export { circleSettings, pointSettings };
